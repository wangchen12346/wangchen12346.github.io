---
title: 常用数据结构
tags: TeXt
article_header:
  type: cover
---


```
python常用的数据结构
# 定义一个列表
# list[]
#index()获取下标值
# append(value)在列表末尾添加元素。
# insert(index,value)在列表指定的位置添加元素。
# del listname[index]从列表中删除一个元素
# pop(index)从列表中删除一个元素
# remove(‘element name’)不知道要从列表中删除的值所处的位置，但知道要删除元素的值。
# a = [1,2]
# print(id(a))
# print(a.pop(-1))
# print(id(a))
# 定义一个字典
# a = dict(one=1, two=2, three=3)
# b = {'one': 1, 'two': 2, 'three': 3}
# c = dict(zip(['one', 'two', 'three'], [1, 2, 3]))
# print(a)



# a == b == c
# 定义一个元组
# tuple
#tuple()

# 不可变序列：字符串、元组（没有增、删、改操作）
# 可变序列：列表、字典（可以对序列执行增、删、改操作，对象地址不发生更改）


# import requests
# repr()
# a=[1,2,3,4,5,6,7]



# del a[2]
# print(a)
# print(a[2])
#
# import json
# with open("jobs.json") as job:
# dic = json.load(job)
# print(dic)
